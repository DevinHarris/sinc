import { createSlice, PayloadAction } from '@reduxjs/toolkit'

const initialState = {
    userWorkout: {}
}

export const userWorkoutSlice = createSlice({
    name: 'userWorkout',
    initialState,
    reducers: {}
})
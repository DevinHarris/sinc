import { PasswordProps } from '@/types'

export const saltAndHashPassword = (password: PasswordProps) => {
    return '// fill in'
}

export const checkPassword = (password: PasswordProps) => {
    return '// fill in'
}